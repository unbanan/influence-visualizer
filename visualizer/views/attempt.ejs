<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <title><%= attempt ? 'Визуализатор попытки' : 'Ошибка' %></title>
    
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <% if (attempt) { %>
        <div id="game-canvas"></div>
        
        <script>
            window.ATTEMPT_DATA = <%- JSON.stringify(attempt) %>;
        </script>
        
        <script type="module" src="/src/main.js"></script>

    <% } else { %>
        <div style="width: 100%; height: 30%; text-align: center; margin-top: 50px; color: white; font-weight: 900; font-family: 'Courier New', Courier, monospace; font-size: 2rem; position: relative; justify-content: center; justify-items: center;">
            <h1>404</h1>
            <p>Такой посылки нет в базе данных.</p>
            <a href="/">Вернуться на главную</a>
        </div>
    <% } %>

</body>
</html>