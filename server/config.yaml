server:
  port: 3000

influencedb:
  host: localhost
  port: 5432
  user: root
  password: INFLUENCE_DB_PASS
  db_name: influencedb
  sslmode: verify-all
  max_open_conns: 50
  max_idle_conns: 10
  conn_max_lifetime: 5m

common:
  player_name_regex: ^[A-Za-z][A-Za-z0-9]{1,20}$
    