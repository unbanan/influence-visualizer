server:
  port: 3000

influencedb:
  host: localhost
  port: 5432
  user: root
  password: INFLUENCEDB_PASS
  db_name: influencedb
  sslmode: disable
  max_open_conns: 50
  max_idle_conns: 10
  conn_max_lifetime: 5m

common:
  player_name_regex: ^[A-Za-z][A-Za-z0-9_]{1,20}$
    